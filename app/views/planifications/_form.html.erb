<%= form_with(model: planification, local: true) do |form| %>
  <% if planification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(planification.errors.count, "error") %> prohibited this planification from being saved:</h2>

      <ul>
      <% planification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year, id: :planification_year %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :planification_user_id %>
  </div>

  <div class="field">
    <%= form.label :possibility_id %>
    <%= form.text_field :possibility_id, id: :planification_possibility_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
